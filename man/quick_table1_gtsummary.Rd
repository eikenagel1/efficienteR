% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quick_table1_gtsummary.r
\name{quick_table1_gtsummary}
\alias{quick_table1_gtsummary}
\title{Quick Table 1 using gtsummary (Medical Journal Standard)}
\usage{
quick_table1_gtsummary(
  df,
  by = NULL,
  ...,
  normality_test = TRUE,
  alpha = 0.05,
  add_p = !missing(by),
  add_overall = TRUE
)
}
\arguments{
\item{df}{Data frame}

\item{by}{Optional grouping variable (unquoted column name) for stratification}

\item{...}{Columns to summarize (unquoted). If none, all columns except 'by'.}

\item{normality_test}{Logical, whether to test normality (default TRUE).
If TRUE, uses Shapiro-Wilk for n < 5000, Anderson-Darling for larger samples.}

\item{alpha}{Significance level for normality test (default 0.05)}

\item{add_p}{Logical, add p-values for group comparisons (default TRUE if 'by' is specified)}

\item{add_overall}{Logical, add overall column when stratifying (default TRUE)}
}
\value{
gtsummary tbl_summary object
}
\description{
Creates a publication-ready Table 1 for medical papers using gtsummary.
For continuous variables:
\itemize{
\item Tests normality using Shapiro-Wilk test
\item Reports mean ± SD if normally distributed (p > 0.05)
\item Reports median \link{IQR} if not normally distributed (p ≤ 0.05)
For categorical variables:
\item Reports N (\%)
Can stratify by a grouping variable with automatic statistical tests.
}
}
\examples{
\dontrun{
# Basic table
quick_table1_gtsummary(mtcars)

# Stratified by group with p-values
quick_table1_gtsummary(mtcars, by = am, mpg, cyl, disp, hp)

# All variables stratified
quick_table1_gtsummary(mtcars, by = vs)
}
}
